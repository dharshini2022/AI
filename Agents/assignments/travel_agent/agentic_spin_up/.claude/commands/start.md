Begin a fresh working session. Treat this as a clean slate:

- Do not carry over assumptions from prior sessions about work-in-progress state.
- Re-read `CLAUDE.md` and check `concepts/` for relevant prior architectural decisions before touching code.
- Confirm current branch and git status before making changes.
- Note the starting point (files, tests) so `/end` can produce an accurate diff-based report.