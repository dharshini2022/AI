[project]
name = "manual-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "dharshinikarthik06@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "groq>=1.7.0",
    "litellm>=1.100.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
manual-agent = "manual_agent:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
