[project]
name = "hands-on"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "dharshinikarthik06@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langgraph>=1.2.11",
]

[project.scripts]
hands-on = "hands_on:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
