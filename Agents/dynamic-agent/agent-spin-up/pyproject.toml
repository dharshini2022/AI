[project]
name = "agent-spin-up"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "dharshinikarthik06@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain-core>=1.6.2",
    "langchain-litellm>=0.7.1",
    "langgraph>=1.2.11",
    "litellm>=1.100.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
agent-spin-up = "agent_spin_up:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
